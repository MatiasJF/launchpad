# dependencies
node_modules/

# builds
.next/
dist/
out/
*.tsbuildinfo

# env (keep .env.example)
.env
.env.local
.env.*.local

# prisma local db
*.db
*.db-journal

# os / editor
.DS_Store

# operator wallet (server key + UTXO ledger) — never commit
*.sqlite
*.sqlite-journal
server-wallet.sqlite*

# fee-calibration probe state (per transaction size)
.fee-calibration*.json
