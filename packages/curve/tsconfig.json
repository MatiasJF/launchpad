{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "noEmit": true,
    "types": ["node"]
  },
  "include": ["src/covenant.ts", "src/index.ts"],
  "comment": "src/contracts/*.ts is sCrypt source (build-time only, needs scrypt-ts); it is compiled in isolation by scripts/compile.sh and excluded from the runtime TS project."
}
