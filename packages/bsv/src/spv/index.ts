/** SPV verification via Merkle proof. Stub — implemented in P3 (BSV-003). */
import { NOT_IMPLEMENTED } from '../notImplemented';

export async function verifyTransaction(_txid: string): Promise<boolean> {
  return NOT_IMPLEMENTED('verifyTransaction (P3 / BSV-003)');
}
