export default function Home() {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', maxWidth: 640, margin: '4rem auto', padding: '0 1.5rem' }}>
      <h1>BSV Launchpad</h1>
      <p>
        Foundation scaffolded. The knowledge base lives in <code>docs/</code> —
        start at <code>CLAUDE.md</code>.
      </p>
      <p style={{ color: '#666' }}>
        Public browse, wallet connect, and the buy flow arrive in P1–P3.
      </p>
    </main>
  );
}
